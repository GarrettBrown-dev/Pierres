@{
    Layout = "_Layout";
}
@model PT.Models.Flavor

<h3>Are you sure you want to remove this?</h3>
<hr />

@Html.DisplayNameFor(model => model.FlavorName): @Html.DisplayFor(model => model.FlavorName)
@using (Html.BeginForm())
{
    <br>
    <input type="submit" value="Delete" class="btn btn-danger btn-sm" />
}
<br>
<p>@Html.ActionLink("Cancel", "Details", null, new {id = Model.FlavorId}, new {@class="btn btn-primary btn-sm"})</p>
